<!-- custom form to deal with escaped HTML characters -->
<%= form_for @container do |f| %>
  <textarea name="container[description]" id="editor1"><%= CGI.unescapeHTML(@container.description).html_safe %></textarea>
  <input name="commit" value="save" class="btn btn-primary" type="submit">
<% end %>