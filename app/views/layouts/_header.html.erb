<div id="menu">
	<ul>
		<% if user_signed_in? %>
			<% if current_user.firstname && current_user.lastname %>
				<li>
					<i class="fa fa-user"></i> <%= current_user.firstname + " " + current_user.lastname %>
				</li>
				<li>
					<%= link_to :logout, destroy_user_session_path, method: :delete  %>
				</li>
			<% end %>
		<% end %>
	</ul>
</div>

<nav>
	<ul>
		<li><%= link_to :containers, containers_path %></li>
		<li><%= link_to :new_container, new_container_path %></li>
	</ul>
</nav>